// para crear una funcion se utiliza el termino 'function'
function saludar(){
    console.log('Hola Mundo')
}

saludar();

function suma() {
    return 2 + 2
}
// piensa que esto es como un for y llamamas a la i, misma logica si llamas a suma te printiara el codigo no el resultado
let resultado = suma();
console.log(resultado)
// una segunda manera de printiarlo
console.log(suma());